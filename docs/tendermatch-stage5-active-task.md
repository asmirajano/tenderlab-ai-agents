# Stage 5 completed checkpoint — stop for review

Approved base: `decd4df7a7ebf67884081301f1e0ae3cdb00ddaa` on
`codex/tendermatch-neon-all-to-all`, in `C:/CodexWork/tendermatch-neon-all-to-all`.

Stage 4 is approved and immutable. Formula run
`8c60354ecfd613fab2edca21528fab8c22aeb275df7115590a5916847c78efc5`
contains 707,660 scored candidates and leaves 1,320,301 outcomes unscored.
Do not rerun source capture, Stage 2/2A/3 or modify Stage 4 code/data/schema.

## Authority preflight

Both remotes fetched/pruned on 2026-09-06. `origin/main` remains
`d230590cf5ee99a679f162b2e3a19b65752c0f16`; approved base is ahead 10 / behind 0,
with no upstream and a clean worktree. The parallel retrieval-ranking worktree
at `C:/Users/Cowork 2/.codex/worktrees/tendermatch-retrieval-ranking` is clean at
that same remote commit. Its code is already inherited by this branch. No merge,
checkout, reset or parallel edit is necessary. Relevant business-table, Formula,
release and original standalone source worktrees are clean and preserved.
The main checkout has unrelated untracked build/deliverable/package-lock/tmp work.
No deployed identity is claimed or needed for this isolated development stage.

## Contract

Canonical owner is `agent:TL-A031`. Primary output: a bounded, inspectable ranked
candidate page for a consultant, with separate immutable Formula metrics and
selected evidence detail. Retrieval is not Match/Non-match, Bid/No-Bid, eligibility,
Data Coverage, Evidence Confidence, human disposition or future AI/TORS output.

Extract reusable retrieval profiles once from sealed Formula input projections
(the supported Stage 2A evidence and pinned Stage 2/3 identities already retained
there). Never enrich or infer unsupported facts. Preserve references and source
status. Hash/version each entity profile and pair-local relevance result.

The existing query-relative rank-fusion policy is preserved. A new versioned
pair-local lexical/structured fallback is additive: corpus-relative ranks would
invalidate unrelated pair values when a profile changes. Rank ordinals are query
positions, not cached source facts. Rank every existing Formula candidate,
including retrieval zero; a bounded shortlist never deletes or filters Formula
history. No embeddings are fabricated.

Read-only target inspection confirmed PostgreSQL 18.6, installed/available
pgvector 0.8.6, zero embedding models and zero vectors. The fallback requires
B-tree focused ranking indexes, not an ANN index without real embeddings.

Target is project `dry-union-87553313`, development branch
`br-polished-boat-b1qddx0m`, direct endpoint
`ep-dark-dew-b15ctyr1.c-5.eu-central-1.aws.neon.tech`, database
`tendermatch_results_dev`, schema `tendermatch_retrieval`. Only guarded results
connections are permitted. The exact owner-only 080 DDL was prepared/tested and
then applied by the orchestrator after action-time approval. Never execute owner
DDL with writer credentials; no further owner action is authorized here.

No frontend, static exports, source connections/writes, model APIs, AI/TORS,
credential/billing changes, push, merge, deployment or publication.

## Historical owner-gate checkpoint

Read-only full experiment and replay completed: each 707,660 candidates, identical
ordered hash `1a7cc57af3cf1d06824700cfc4c367658b9abc67519d372a8e77fc1f4c123b47`.
Run `b0ba6e965bc81ef5f3d53466d815c1c7c4c4596bd21b16ff9342b4098b484c7d`.
24,007 positive relevance / 683,653 zero relevance; no candidate discarded.
17,450 profiles, with zero extraction on unchanged profile replay. At that earlier
owner gate no Stage 5 Neon tables or rows had been created. The exact 080 owner
runbook and synthetic SQL persistence/replay/security/index benchmark were ready.
Full tests/build passed 556/556, zero skips; full lint and strict new-module
TypeScript passed. Fourteen new Stage 5 files were staged but uncommitted at that
historical checkpoint. The continuation below supersedes that pause.

## Completed authorized persistence

The orchestrator confirmed owner execution of all 25 statements, including COMMIT,
in the verified target. Action-time writer inspection confirmed the 080 migration
marker and five empty owner-owned/RLS-enabled tables. Both code hashes exactly
match the approved experiment. Guarded writer execution completed: 17,450 profiles,
17,450 memberships, 707,660 pairs, one run and one sealed completion. Full readback
and unchanged cache replay match the earlier outcome hash; no recalculation or
duplicate insertion occurred on replay. No owner DDL was run by this worker.

Independent read-only validation confirms 707,660 unique pairs, zero invalid
components/missing Formula links, 17,450 reused profiles with no extraction,
complete ordered/unique supplier and tender traversal, correct selected detail,
SELECT/INSERT-only RLS tables and unchanged Stage 4 completion. Zero model/source
connections, zero audit writes. Evidence is in the Stage 5 execute/validation JSON
and `docs/evidence/tendermatch-stage5-retrieval.md`. Stage 5 implementation hash
remains `ff30b52ab11a8789cae5d9e5c30cb74153ce56a6a93e77c9039ec9a5dbbc27f5`.

The local commit containing this checkpoint is the Stage 5 handoff. Review its
final test/build/security evidence and exact commit SHA reported by the worker.
Final full tests: 557/557, no failures/skips; all three builds and 64 Agent
Specifications passed. Full lint, strict Stage 5 module TypeScript, staged
whitespace and the 17-file secret-pattern scan passed. Final remote/relevant
parallel state remains as recorded in the evidence report; no parallel work changed
in this worktree.
Stop for orchestrator review. Do not start another stage or modify sealed data.
